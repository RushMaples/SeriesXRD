# Python bytecode and caches
__pycache__/
.pytest_cache/
*.py[cod]
*.egg-info/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# Local agent/editor settings
.claude/
.codex_tmp/
.codex_worktrees/

# Machine-specific generated files (written at runtime; they contain local
# absolute paths and must not be committed —
# see examples/calibration_session_config.example.json for the schema)
calibration_session_config.json
reduction_session_config.json
analysis_session_config.json
last_preflight.json
calibration_handoff.json
master_metadata.json

# Generated run outputs (written at runtime)
data/
figures/
metadata/
logs/
previews/
outputs/

# Logs and editor settings
*.log
worker_gen*.json
.vscode/
.idea/
.DS_Store
reference_phases/

**/data/processed/
**/data/raw/
**/figures/
**/metadata/
**/accepted_calibrations/
*.tif
*.tiff
*.edf
*.npy
*.npz
*.csv


# Manuscript working files — drafts, figures, benchmark scratch
paper/

# Correlation-mapping prototype: code is tracked, local research data is not.
prototypes/correlation_mapping/results/
prototypes/correlation_mapping/incoming/
prototypes/correlation_mapping/UOTe*/
prototypes/correlation_mapping/uote_xy_handoff*/
prototypes/correlation_mapping/correlation-explorer/node_modules
prototypes/correlation_mapping/correlation-explorer/dist/
prototypes/correlation_mapping/correlation-explorer/data/*.json
prototypes/correlation_mapping/correlation-explorer/*.tsbuildinfo
