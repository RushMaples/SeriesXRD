# Conda environment for the seriesxrd workflow.
# pyFAI and its compiled dependencies install most reliably from conda-forge
# (this matches the conda-forge package names used in seriesxrd/core/env.py).
#
# Create with:  conda env create -f environment.yml
# Activate:     conda activate seriesxrd
name: seriesxrd
channels:
  - conda-forge
dependencies:
  - python>=3.10
  - numpy
  - scipy
  - pillow
  - matplotlib
  - pyfai
  - fabio
  - h5py
  - tk
  # optional image/stack readers
  - tifffile
  - imageio
  - hdf5plugin
  # notebook front-end
  - jupyterlab
