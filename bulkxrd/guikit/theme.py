"""Dark Catppuccin Mocha palette shared by the Tk GUIs and matplotlib figures."""
from __future__ import annotations

# Widget / figure surfaces
BG       = "#1e1e2e"   # window + figure background
BG2      = "#2a2a3e"   # raised panels, axes background
FG       = "#cdd6f4"   # default text / ticks
ACCENT   = "#89b4fa"   # blue highlight
ACCENT2  = "#a6e3a1"   # green highlight / "ok"
WARN     = "#f38ba8"   # red warning
BORDER   = "#45475a"
ENTRY_BG = "#313244"
BTN_BG   = "#363650"
BTN_ACT  = "#45475a"
MUTED    = "#9399b2"   # secondary/help text (WCAG contrast)

# Line-plot trace colors
CLR_RAW  = "#89b4fa"   # blue  — raw / unmasked
CLR_MSKD = "#a6e3a1"   # green — masked / filtered
CLR_DIFF = "#f38ba8"   # red   — difference
CLR_SMTH = "#fab387"   # peach — smoothed / fit
CLR_REF  = "#f5c2e7"   # pink  — reference lines (calibrant peaks)
